import React from "react";

const Navbar = () => {
  return (
    <div className="w-full h-14 bg-gray-900 text-white flex items-center px-4 shadow-md">
      <h1 className="text-lg font-semibold">❓ FIRIBGARMI </h1>
    </div>
  );
};

export default React.memo(Navbar);
